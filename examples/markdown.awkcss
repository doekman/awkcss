{
	width(120);
}
/^#+ / { 
	color("yellow");
	font_weight("bold");
	width();
}
/^# / { 
	background_color("blue");
}
/^## / { 
	text_decoration("underline");
}
/^ {4,}/ {
	text_decoration(blink);
	color("gray");
}
